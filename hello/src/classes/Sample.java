package classes;

public class Sample {
	public static void main(String[] args) {
		int price = Int.
		int a= price / 50000;
	    int b= price % 50000 / 10000;
	    int c= price % 10000 / 5000;
	    int d= price % 5000 / 1000;
	    int e= price % 1000 / 500;
	    int f= price % 500 / 100;
	    int g = price % 100;
	    int total = a + b + c + d + e + f + g;
				
	    		
	    System.out.printf("50000" x %", a);
	    
	}
}